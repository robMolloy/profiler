export default class methodIterator {
  constructor(methods, i) {
    Object.assign(this, { methods, i });
    //call stuff
  }

  createMethodIterator() {
    //do stuff
    this.methods.do();
  }
}
